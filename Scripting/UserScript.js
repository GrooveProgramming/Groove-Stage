// Function for ticking the user scripts. We could have an actuall tick function for the user or just execute the code.
// The delta time property is useful for frame rate independance. For example you could say `tick: move (10 * deltaTime) steps`. This would move 10 steps every second.
// TODO write script.
function tickUserScripts(){
	
}